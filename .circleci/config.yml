version: 2
orbs:
  node: circleci/node@4.5.1
jobs:
  example-job:
    docker:
      # replace with your preferred image
      - image: cimg/base:stable
    steps:
      - node/install
workflows:
  example-workflow:
    jobs:
      - example-job